# Cursor Lock Basic

Demonstrates `m-mouse-behavior` with three modes:

- unlocked: Normal system cursor
- window: Hide system cursor; draw a dot, constrained to window
- locked: Pointer lock, cursor hidden and centered (FPS-style)

Use the `m-overlay` buttons to switch modes. For locked mode, click the scene to enter pointer lock.

 