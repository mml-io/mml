# Rotate With Camera Demo

Demonstrates the `rotate-with-camera` attribute on `m-character-controller`. A player character spawns, and an `m-overlay` UI lets you toggle camera-follow rotation on/off.

- Toggle: Use the Overlay button "Rotate Cam" to enable/disable the attribute
- Behavior: When enabled, the character smoothly rotates to match the orbit camera yaw
